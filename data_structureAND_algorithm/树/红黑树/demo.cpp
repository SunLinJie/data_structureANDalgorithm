#include <set>
#include <map>

using namespace std;

int main()
{
    //redblack Tree ; STL;
    set<int> a;
    multiset<int> b;
    map<int, int> c;
    multimap<int,int> d;

    a.insert(50);
    a.insert(40);
    a.insert(30);

    return 0;
}
